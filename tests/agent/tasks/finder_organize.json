{
  "id": "finder-organize",
  "name": "List desktop files and create inventory note",
  "goal": "List all files on the Desktop using Finder, then create a note in Notes app titled 'Agent Test - Desktop Inventory' that contains the actual file names and their kinds (e.g., 'screenshot.png - PNG image'). The note must list real files, not made-up names.",
  "setup": [
    [
      "/bin/sh",
      "-c",
      "set -e; d=\"$HOME/Desktop\"; printf \"quarterly figures\\n\" > \"$d/cu-l2-fixture-report.txt\"; printf \"meeting notes\\n\" > \"$d/cu-l2-fixture-notes.md\"; mkdir -p \"$d/cu-l2-fixture-archive\""
    ]
  ],
  "verify": [
    {
      "description": "Note exists",
      "command": [
        "cu",
        "tell",
        "Notes",
        "get name of note \"Agent Test - Desktop Inventory\""
      ],
      "expect_contains": "Desktop Inventory"
    },
    {
      "description": "Note contains actual desktop file names",
      "cross_check": {
        "source_command": [
          "cu",
          "tell",
          "Finder",
          "get name of first file of desktop"
        ],
        "target_command": [
          "cu",
          "tell",
          "Notes",
          "get plaintext of note \"Agent Test - Desktop Inventory\""
        ],
        "description": "Note must contain at least one real file name from the Desktop"
      }
    }
  ],
  "cleanup": [
    [
      "/bin/sh",
      "-c",
      "set -e; d=\"$HOME/Desktop\"; rm -f \"$d/cu-l2-fixture-report.txt\" \"$d/cu-l2-fixture-notes.md\"; if [ -d \"$d/cu-l2-fixture-archive\" ]; then rmdir \"$d/cu-l2-fixture-archive\"; fi; for p in \"$d/cu-l2-fixture-report.txt\" \"$d/cu-l2-fixture-notes.md\" \"$d/cu-l2-fixture-archive\"; do [ ! -e \"$p\" ] || { echo \"cleanup left $p\" >&2; exit 1; }; done"
    ]
  ],
  "cleanup_until": [
    {
      "command": [
        "cu",
        "tell",
        "Notes",
        "set noteIDs to get id of every note whose name is \"Agent Test - Desktop Inventory\"\nif (count of noteIDs) is greater than 0 then\n  set stableID to item 1 of noteIDs\n  delete note id stableID\nend if\nreturn \"cleaned\""
      ],
      "verify": [
        "cu",
        "tell",
        "Notes",
        "set remaining to count of (every note whose name is \"Agent Test - Desktop Inventory\")\nif remaining is not 0 then error \"cleanup left \" & remaining & \" inventory note(s)\"\nreturn \"cleaned\""
      ]
    }
  ]
}
